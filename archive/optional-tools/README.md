# Optional Tools

本目录保留历史上的 Python 脚本（风格/引用辅助）与配套测试。

- `src/blog_pipeline/` — 可选的分析脚本；使用前请确认依赖并在 `state/LOG.md` 记录。
- `tests/` — 如需运行 pytest，可在本目录下执行。

> 默认流程通过 `/start-blog` 多代理协作完成；这些脚本仅供参考，不再是守门条件的一部分。
